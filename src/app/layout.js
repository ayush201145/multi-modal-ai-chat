import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Nexus Chat — Multi-Model AI Interface",
  description:
    "A unified chat interface aggregating OpenAI, Anthropic, Google Gemini, and OpenRouter models with real-time streaming.",
};

export default function RootLayout({ children }) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased dark`}
    >
      <body className="min-h-full flex flex-col bg-[#0a0b14] text-slate-100 overflow-hidden">
        {children}
      </body>
    </html>
  );
}
